apiVersion: v1
kind: Service
metadata:
  name: {{ include "vehicle_simulator.fullname" . }}
  labels:
    {{- include "vehicle_simulator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vehicle_simulator.selectorLabels" . | nindent 4 }}
