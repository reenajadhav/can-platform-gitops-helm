apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "vehicle-simulator.fullname" . }}-test-connection"
  labels:
    {{- include "vehicle-simulator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "vehicle-simulator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
